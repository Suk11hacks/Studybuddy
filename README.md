# MCQ to Notes Generator

This app takes MCQ-style inputs and uses an open-source LLM to generate detailed notes including tests, reagents, and mechanisms.

## 💡 Features
- Open-source local inference (via Colab)
- Optionally fetch source info from web
- Streamlit-based interface

## 🛠 Run Locally
```bash
pip install -r requirements.txt
streamlit run app.py
